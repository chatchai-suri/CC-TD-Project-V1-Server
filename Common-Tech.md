# 📑 คัมภีร์สรุปรวบรวมคำสั่ง Prisma ORM (กันลืมฉบับป๋าปู)

ไฟล์นี้จัดทำขึ้นเพื่อเป็นคู่มือเปิดดูด่วน (Cheatsheet) สำหรับลำดับการใช้คำสั่ง Prisma ในการบริหารจัดการโครงสร้างฐานข้อมูลระหว่างเครื่องเขียนโค้ด (Mac - pp2) และเครื่องเก็บข้อมูลจริง (Windows - pp1)

---

## 🔄 1. แผนผังลำดับการใช้งาน (Prisma Workflow Lifecycle)
เมื่อเริ่มต้นโปรเจกต์ใหม่ หรือมีการปรับปรุงตารางข้อมูลกอล์ฟ ให้เดินตามเส้นทางนี้เสมอ:

[สเต็ป 1: สร้างห้อง] รัน npx prisma init (ทำครั้งแรกครั้งเดียว)

[สเต็ป 2: วาดพิมพ์เขียว] ไปออกแบบ Model ตารางในไฟล์ prisma/schema.prisma

[สเต็ป 3: ลุยหน้างานจริง] รัน npx prisma db push หรือ migrate dev
(เพื่อส่งโครงสร้างตารางข้ามเน็ตเวิร์กมุดท่อไปสร้างใน MySQL บน pp1)

[สเต็ป 4: อัปเดตสมองคอม] รัน npx prisma generate
(เพื่อให้เครื่อง Mac รู้จักคำศัพท์และฟิลด์ใหม่ ๆ เวลาเขียนโค้ด)

---
## 🗂️ 2. เจาะลึกความหมายและหน้าที่ของแต่ละคำสั่ง

### 🔲 คำสั่งที่ 1: `npx prisma init` (ปั้นดินให้เป็นดาว)
* **หน้าที่:** ใช้สำหรับจุดชนวนเริ่มต้นระบบ Prisma ในโปรเจกต์[cite: 12]
* **ผลลัพธ์บนเครื่อง:** จะงอกโฟลเดอร์ `prisma/` และไฟล์พิมพ์เขียว `prisma/schema.prisma` ขึ้นมาให้โดยอัตโนมัติ[cite: 12]
* **ความถี่ในการใช้:** **ทำแค่ 1 ครั้งถ้วน** ตอนตั้งไข่โปรเจกต์[cite: 12]

### 💻 คำสั่งที่ 2: `npx prisma generate` (อัปเดตสมองเครื่อง Mac)
* **หน้าที่:** สร้างและอัปเดตตัว "วุ้นแปลภาษา" หรือ Prisma Client ในเครื่อง Mac[cite: 12, 13]
* **ผลลัพธ์บนเครื่อง:** โค้ดในโฟลเดอร์ `@prisma/client` จะถูกอัปเดตตามไฟล์พิมพ์เขียวล่าสุด[cite: 12] ทำให้หน้าจอ VS Code รู้จักคำสั่งเช่น `prisma.course.findMany()` และเคลียร์เส้นหยักแดงเตือนหลอกตาให้หายวับไป[cite: 7, 12]
* **พื้นที่ส่งผล:** **ทำงานอยู่ภายในเครื่อง Mac (pp2) ของป๋าเท่านั้น** ไม่ได้วิ่งไปยุ่งกับฐานข้อมูลที่บ้าน[cite: 11, 12]

### 🛢️ คำสั่งที่ 3: `npx prisma db push` (สายลุยหน้างาน / ทำห้องแล็บ)
* **หน้าที่:** ส่งโครงสร้างจากไฟล์พิมพ์เขียวในเครื่อง Mac พุ่งตรงไปปรับรูปตารางข้อมูลใน MySQL ของจริง[cite: 11, 12]
* **ผลลัพธ์บนเครื่อง:** เกิดตาราง (Tables) และความสัมพันธ์จริงในระบบดาต้าเบส[cite: 11, 12]
* **ข้อดี:** ทำงานเร็วมาก ไม่เก็บประวัติย้อนหลัง เหมาะสำหรับการ Dev นอกสถานที่ หรือปรับแก้ตารางไปมาในเฟสเริ่มแรก[cite: 11]
* **พื้นที่ส่งผล:** **วิ่งทะลวงท่อ Tailscale ข้ามมิติไปเปลี่ยนก้นถังข้อมูลที่เครื่อง Windows (pp1)**[cite: 11, 12]

### 📜 คำสั่งที่ 4: `npx prisma migrate dev` (สายระเบียบ / ขึ้นระบบจริง)
* **หน้าที่:** ทำหน้าที่ส่งโครงสร้างตารางไปที่ฐานข้อมูลจริงเหมือน `db push` แต่จะทำการ **"สลักประวัติศาสตร์บันทึกการแก้ไข"** ลงในโฟลเดอร์ `prisma/migrations` ไว้ด้วย[cite: 11]
* **ผลลัพธ์บนเครื่อง:** เกิดโฟลเดอร์บันทึกประวัติ และไฟล์ `migration_lock.toml` เพื่อล็อกเวอร์ชันตาราง[cite: 11, 13]
* **ข้อดี:** ปลอดภัยสูง ทำให้ทีมหรือตัวป๋าเองรู้ว่าสัปดาห์ก่อนหน้าเรามีการปรับโครงสร้างฟิลด์อะไรไปบ้าง[cite: 11]

---
## 💡 3. สูตรท่องจำสูดโอโซนริมฝั่งโขง (สรุปเปรียบเทียบ)
> 🥊 **`db push` / `migrate dev`** = ฝ่ายบู๊ วิ่งอ้อมโลกไปสั่ง **เปลี่ยนรูปร่างถังข้อมูลจริงหลังบ้าน (pp1)**[cite: 11, 12]

> 🧠 **`generate`** = ฝ่ายบุ๋น เดินไปบอกสมองเครื่องคอมพิวเตอร์ว่า **จำฟิลด์ใหม่ไว้เอาไปเขียนโค้ดหน้าบ้าน (pp2)**[cite: 11, 12]
---