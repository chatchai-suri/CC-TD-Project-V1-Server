## TD-Basic-Tech.md is common & basical techincal or Q&A of technical stack
### Prisma Schema
* model name VS table name (การกำหนดชื่อtable ให้แตกต่างจาก ชื่อmodel)
1. Model name: User vs Table name: user

ในโลกการออกแบบคลังข้อมูลของ Prisma มีข้อตกลงระดับสากล (Convention) ที่กำหนดไว้เพื่อลดความสับสนของนักพัฒนาครับ:

    * ในไฟล์ schema.prisma: บังคับให้ใช้ อักษรตัวพิมพ์ใหญ่ตัวแรก (User, Course) เพื่อระบุชื่อโมเดล (Model Name) ตามระเบียบการเขียน Schema

    * ในโค้ด JavaScript / TypeScript: เมื่อป๋ารันคำสั่ง npx prisma generate ตัว Prisma Client จะทำการสร้างสมองกลแปลงร่าง (Auto-mapping) สลับชื่อเหล่านั้นให้กลายเป็น อักษรตัวพิมพ์เล็กตัวแรก (user, course) ในฐานะฟังก์ชันเรียกใช้งาน (Property/Method) ทันทีครับ

💡 เหตุผลทางสถาปัตยกรรม: เพราะในภาษา JavaScript/TypeScript การเรียกใช้คุณสมบัติของ Object นิยมใช้รูปแบบตัวพิมพ์เล็ก (camelCase) เช่น prisma.user หรือ prisma.course เพื่อให้โค้ดดูสะอาดและถูกหลักสากลนั่นเองครับป๋า
``` prisma
model User {
  user_id       Int            @id @default(autoincrement())
  username      String?        @unique
  password      String?
  fullname      String
  nickname      String?
  phone_number  String?
  age           Int?           // 👈 หยอดฟิลด์อายุประจำตัวนักกอล์ฟ (เผื่อดึงไปคำนวณ Modified-Peoria)
  global_role   String         @default("golfer")
  created_at    DateTime       @default(now())
  
  // Relations
  flights_joined FlightMember[]
}
```
2. Model name: User vs Table name: users

ถ้าในอนาคตป๋าอยากกำหนดชื่อเรียก "ในตู้ MySQL" ให้ต่างออกไป ด้วยคำสั่งอักขระ @@map() บล็อกไว้ด้านล่างสุดของโมเดลได้ดังนี้ครับ:
``` prisma
model User {
  user_id     Int    @id @default(autoincrement())
  username    String @unique
  // ... ฟีลด์อื่น ๆ ของป๋า
  
  @@map("users") // 👈 คำสั่งนี้จะบอกระบบว่า ใน MySQL pp1 ให้สร้างชื่อตารางว่า "users" (ตัวเล็กมี s) นะ!
}
```
## 🧩 สรุปคลังความรู้เชิงสถาปัตยกรรม (Session 1 - 3 Recap)

### 1. ปรัชญา "Declarative & Bi-directional Relations"
* **แก่นความคิด:** ระบบ Prisma ORM บังคับให้การผูกสัมพันธ์ข้อมูลต้องประกาศแบบสองทิศทางเสมอ เมื่อตารางลูก (`Score`) ประกาศชี้เป้าหาตารางแม่ (`User`, `Hole`) ฝั่งตารางแม่ก็ต้องงอกฟีลด์พหูพจน์ `[]` มารองรับสัญญานขาลุยย้อนศรกลับไปเสมอเช่นกัน เพื่อให้โค้ดฝั่งหลังบ้านสามารถส่องหาข้อมูลข้ามท่อกลับไปกลับมาได้อย่างอิสระ (Bi-directional)
* **ปุ่มลัดเนรมิตโค้ด:** บนเครื่อง Mac (`pp2`) ให้ป๋าพิมพ์ฝั่งตารางลูกเสร็จ แล้วกดปุ่มลัด **`Shift + Option + F`** ตัวสมองกล Extension Prisma จะวิ่งไปพิมพ์งอกโค้ดฝั่งตรงข้ามที่ตารางแม่ให้อัตโนมัติในเสี้ยววินาทีครับ!

### 2. กลยุทธ์ "Direct Relation" เพื่อ Performance Index สูงสุด
* **สเปกปัญหา:** หากเราออกแบบฐานข้อมูลให้วิ่งอ้อมท่อหลายทอด (เช่น คะแนน ➡️ ก๊วน ➡️ สมาชิก ➡️ ยูสเซอร์) เวลาเขียนคำสั่ง Async คิวรี่ข้อมูล ตัวเครื่องยนต์หลังบ้านจะต้องคีย์คำสั่ง SQL `JOIN` ซ้อนกันหลายเลเยอร์ ทำให้ระบบทำงานหนักและประมวลผลช้าลงอย่างเห็นได้ชัด
* **สเปกแก้ไข:** หันมาใช้กลยุทธ์ **Direct Relation (สะพานสายตรง)** โดยยอมฝังคีย์ `user_id` และ `hole_id` ล็อกตรงไว้ที่ก้นตาราง `Score` เลย เพื่อให้คิวรี่ข้อมูลจบในเลเยอร์เดียว ประสิทธิภาพความเร็วพุ่งแรงสูงสุดเกรดอุตสาหกรรม

### 3. คีย์คู่ฝั่งตารางตัวกลาง จำเป็นต้องทำ @@unique ไหม?
* **คำวินิจฉัย:** จำเป็นอย่างยิ่งครับ! ในกรณีที่มีคีย์ต่างแดน (Foreign Key) ตกสลักอยู่คู่กันในตารางสะพานเชื่อม (เช่น `flight_id` และ `user_id` ในตาราง `FlightMember`) เราต้องประกาศข้อกำหนด **`@@unique([flight_id, user_id])`** 
* **ประโยชน์สูงสุด:** คำสั่งนี้เรียกว่า **Composite Unique Key (UK)** มีไว้เพื่อทำหน้าที่เป็นโล่ป้องกันระดับ Database ดักจับไม่ให้นักกอล์ฟไอดีเดิม สามารถหลุดเข้าไปลงทะเบียนซ้ำซ้อนในกลุ่มก๊วน Flight เดียวกันได้เกิน 1 ครั้งนั่นเองครับป๋าปู

### 4. ความต่างชั้นเลเยอร์ของ @@map() กับชื่อโมเดล
* **ระดับแอป (TypeScript Application Layer):** เราตั้งชื่อคำนามเอกพจน์ตัวพิมพ์ใหญ่ตัวแรกเสมอ เช่น `model User`, `model Course` เพื่อให้โค้ดเวลาเขียนฟังก์ชันสะอาดสั้นกระชับ สไตล์ `prisma.user.create()`
* **ระดับฐานข้อมูล (Physical MySQL Layer):** แต่ในตู้ MySQL เราใช้เก็บก้อนพัสดุหลายก้อน จึงนิยมตั้งชื่อเป็นพหูพจน์ตามหลักสากล เราจึงใส่สัญลักษณ์คำสั่ง **`@@map("users")`** ไว้ก้นโมเดล เพื่อสั่งให้ Prisma แอบไปงอกชื่อตารางใน MySQL ให้มี `s` พ่วงท้าย โดยไม่กระทบกับความสะอาดสั้นคลีนของโค้ดใน VS Code ครับป๋า!

## 🧩 Session 4: คัมภีร์สรุปวินัยฐานข้อมูลสไตล์ Simply Standard

### 1. ปรัชญา "Declarative & Bi-directional Relations"
* **นิยาม:** คือความสัมพันธ์แบบสองทิศทางโปร่งใส เมื่อเราสร้างสะพานเชื่อมจากตารางลูก (เช่น Score) ไปหาร่างตารางแม่ (เช่น User) ในไฟล์ Prisma บังคับให้เราต้องเขียนระบุท่อรับสัญญาณย้อนกลับที่ฝั่งตารางแม่ด้วยเสมอ เพื่อให้โค้ดหลังบ้านเรียกสั่งงานค้นหาแบบพ่วงข้อมูลย้อนศร (เช่น ดึง User พร้อมสั่ง include คะแนนทั้งหมด) ได้อย่างราบรื่น
* **คีย์ลัดค้ำยัน (Shortcut):** ป๋าไม่ต้องพิมพ์ฝั่งย้อนศรเองให้เมื่อยมือ บนเครื่อง Mac ให้กดปุ่มลัด **`Shift + Option + F`** ตัว Extension Prisma จะทำการวิ่งไปงอกรหัสฝั่งตรงข้ามให้อัตโนมัติทันทีใน 0.1 วินาทีครับ!

### 2. กฎทองคำเรื่องการดัก Direct Relation
* **วินัยประสิทธิภาพ:** หากระบบมีความสัมพันธ์ที่ต้องวิ่งหาข้อมูลอ้อมโลกเกิิน 2 ทอด (เช่น คะแนน ➡️ ก๊วน ➡️ สมาชิกก๊วน ➡️ นักกอล์ฟตัวจริง) จะทำให้เซิร์ฟเวอร์เหนื่อยกับการสั่ง SQL JOIN หลายเลเยอร์ ส่งผลให้ฟังก์ชัน Async ประมวลผลช้าลง 
* **แนวทางแก้ไข:** ให้สลักฟีลด์ไอดีผูกตรงแบบ **`Direct Relation`** (เช่น ยัด `user_id` และ `hole_id` ลงในตาราง `Score` โดยตรง) เพื่อสร้างสะพานสายตรงดึงข้อมูลไวที่สุดใน 0.001 วินาทีครับป๋าปู

### 3. มีคีย์ FK คู่ในตารางเดียวกัน จำเป็นต้องทำ @@unique ไหม?
* **คำถามของป๋า:** *"หากมี FK 2 field ในตารางเดียวกัน (เช่น กรณี ตาราง FlightMember) ต้องทำ relation field @@unique([flight_id, user_id]) นี่ป๋าเข้าใจถูกใหม?"*
* **คำวินิจฉัยแก้ไข:** ป๋าเข้าใจลอจิกถูกต้องเป๊ะครับ! แต่เจ็มคุงขอตบแต่งชื่อเรียกทางวิชาการนิดเดียวครับป๋า ตัว `@@unique` เราจะเรียกว่าเป็นคำสั่งสร้าง **`Composite Unique Key (UK)` ดักระดับตาราง** ไม่ใช่คำสั่งทำ Relation ครับ 
* **ประโยชน์สูงสุด:** ใส่เพื่อดักไม่ให้ข้อมูลคู่หูนี้หลุดซ้ำซ้อนกันในถัง MySQL (เช่น ดักจับไม่ให้ นักกอล์ฟไอดี 1 สามารถแอบลงทะเบียนซ้ำซ้อนในก๊วน Flight A1 เดียวกันได้เกิน 1 ครั้งนั่นเองครับป๋าปู)

### 4. วินัยการสลักป้ายชื่อ @@map() ประจำตาราง
* **สากลนิยมสไตล์:** ในโมเดลภาษา TypeScript เรานิยมใช้คำนามเอกพจน์ตัวพิมพ์ใหญ่ตัวแรก เช่น `model User` หรือ `model Course` เพื่อให้โค้ดฝั่งเขียนแอปสะอาดสะอ้าน 
* **พฤติกรรมในตู้ MySQL:** แต่ในระบบตู้ฐานข้อมูล ตัวตารางจริง ๆ มันใช้เก็บวัตถุสิ่งของหลาย ๆ ชิ้นรวมกัน จึงนิยมตั้งชื่อเป็นคำนามพหูพจน์ (มี s พ่วงท้าย) เราจึงต้องติดป้ายอักขระ **`@@map("users")`** หรือ **`@@map("courses")`** ไว้ล่างสุดของโมเดล เพื่อแปลงโฉมตารางก้นถังใน MySQL ให้สวยงามและถูกหลักวินัยสากลเป๊ะ ๆ ครับป๋า!

### 5. คัมภีร์ลึกซึ้ง Prisma ORM & สถาปัตยกรรมฐานข้อมูล (Session 4.5)

#### 5.1 ปรัชญา "All-or-Nothing" ด้วย prisma.$transaction(async (tx) => {})
* **นิยามเชิงลึก:** คือระบบการบริหารจัดการธุรกรรม (Database Transaction) แบบ "ทำครบทุกข้อ หรือไม่ทำเลย" โดยตัวแปร `tx` คือตัวแทนสมองกลของ Prisma ที่เข้ามาจองท่อทางเดินข้อมูลไว้ชั่วคราว
* **หน้าที่และกลไก:** ใช้ในกรณีที่ต้องเขียนข้อมูลลงหลายตารางพร้อมกันและมีความสัมพันธ์ผูกตึกกันอยู่ (เช่น สร้างกลุ่ม Flight ก่อน แล้วเอาไอดีไปยัดลง FlightMember) หากคำสั่งใดคำสั่งหนึ่งด้านในเกิดเอเรอร์พังกลางคัน ระบบจะทำการ **Rollback (ย้อนเวลากลับ)** ลบข้อมูลที่บันทึกไปก่อนหน้าในธุรกรรมนั้นออกทั้งหมดทันที เพื่อป้องกันปัญหาข้อมูลกำพร้าหรือข้อมูลขยะค้างถัง (Data Inconsistency)
* **กฎเหล็กการเขียน:** ด้านในบล็อกต้องเปลี่ยนมาเรียกใช้งานผ่านตัวแปร `tx` เสมอ (เช่น `tx.flight.create`) ห้ามใช้คำว่า `prisma` ดั้งเดิมเด็ดขาด

#### 5.2 กลยุทธ์ความเร็วสูงด้วยคำสั่ง .createMany()
* **รูปแบบอาร์กิวเมนต์:** ฟังก์ชัน `createMany` บังคับรับข้อมูลเป็น **Array ของ Object** เท่านั้น (เช่น `[{ field1: value }, { field1: value }]`)
* **ประโยชน์สูงสุด:** เป็นการส่งก้อนพัสดุพหูพจน์ยิงโครมเดียวทะลวงท่อ OpenSSH ไปสลักลงตู้ MySQL ฝั่งเครื่องบ้าน `pp1` พร้อมกันหมด ประหยัดรอบการวิ่งของเน็ตเวิร์ก (Network Round-trip) ได้มหาศาลเมื่อเทียบกับการวนลูปสั่ง `create` ทีละแถว

#### 5.3 รหัสลับเรดาร์ "members" ในคำสั่ง include (Prisma Naming Convention)
* **ความต่างระว่าง ER กับ โค้ด:** ในแผนภาพ ER Diagram หรือในตู้ SQL จริง ๆ จะไม่มีคอลัมน์ชื่อ `members` ปรากฏอยู่บนตาราง `Flight` 
* **กลไกเบื้องหลัง:** คำว่า `members` คือ **Relation Field (ชื่อเรดาร์ข้ามมิติ)** ที่เราตั้งขึ้นเองในไฟล์ `schema.prisma` ฝั่ง `model Flight` เพื่อบอกให้ Prisma รู้ว่าก๊วนนี้สามารถเชื่อมโยงเรดาร์ย้อนศรไปหาแถวข้อมูลในตาราง `FlightMember` ได้ ซึ่งทำให้ไวยากรณ์ใน VS Code สะอาดสะอ้านกว่าการเขียน `JOIN` ใน SQL ดั้งเดิมมาก

#### 5.4 มาตรฐานความปลอดภัย (Security Best Practice) เรื่องการดึงข้อมูล (select & omit)
* **ความเสี่ยงหน้างาน:** การสั่ง `include: { user: true }` จะเป็นการดึงข้อมูลทุกคอลัมน์ของนักกอล์ฟคนนั้นออกมาทั้งหมด ซึ่งจะส่งผลให้ฟีลด์อันตรายอย่าง `password` หลุดติดท่อ API ออกไปหน้าบ้าน React ด้วยเสมอ
* **ทางแก้ไขระดับแอป (Working Rule):** 1. ให้เปลี่ยนมาใช้คำสั่ง **`select`** ซ้อนด้านในเพื่อเลือกเจาะจงเฉพาะคอลัมน์ที่จำเป็นและปลอดภัยเท่านั้น (เช่น `user_id: true, username: true`)
  2. ใน Prisma 7 มีคำสั่ง **`omit`** มาช่วยทำงานย้อนศร คือระบุฟีลด์ที่ "ห้ามเอามา" เช่น `omit: { password: true }` เพื่อความสะดวก
  * ⚠️ *ข้อพึงระวัง:* ห้ามลืมคุมระเบียบข้อนี้ในทุกเส้นท่อ API ที่มีการดึงข้อมูลผู้ใช้งานเด็ดขาด เพื่อสุขอนามัยความปลอดภัยเกรดอุตสาหกรรม

#### 5.5 ถอดรหัสหัวเจาะสมองกลคำสั่ง .upsert()
* **นิยามและกลไก:** มาจากคำว่า **Up**date + In**sert** ควบรวมกัน ทำหน้าที่เป็นลอจิกเงื่อนไข (If-Else) สำเร็จรูปในคำสั่งเดียว ประกอบด้วย 3 องค์ประกอบหลัก:
  1. **`where`:** ทำหน้าที่คอยส่องเรดาร์หาข้อมูลเก่าในตาราง (ตรวจสอบจากคีย์หลักหรือ Composite Unique Key) โดยใช้เงื่อนไขคีย์หลักหรือคีย์ Unique ว่า "เคยมีคะแนนของนักกอล์ฟคนนี้ ในหลุมนี้ ในก๊วนนี้ บันทึกไว้แล้วหรือยัง?"
  2. **`update`:** บล็อกคำสั่งที่จะทำงานก็ต่อเมื่อ `where` ส่องเจอข้อมูลเก่าอยู่แล้ว (เช่น คีย์แต้มซ้ำหลุมเดิม ระบบจะยอมให้เขียนแต้มใหม่ทับแต้มเก่าทันที)
  3. **`create`:** บล็อกคำสั่งที่จะทำงานก็ต่อเมื่อ `where` ส่องแล้วเจอแต่ความว่างเปล่า (เป็นการคีย์แต้มหลุมนั้นครั้งแรกของวัน ระบบจะสลักแถวข้อมูลใหม่ลงตู้ทันที)

#### 5.6 ความลึกลับของเลขทะเบียน ID กระโดดข้ามพิกัด (Auto Increment Behavior)
* **กลไกหน้างาน:** ในฐานข้อมูลระบบ InnoDB (MySQL) เมื่อมีการส่งคำสั่งเขียนข้อมูล (`.create` หรือ `INSERT`) สมองกลคุมตู้จะทำการหมุนวงล้อแจกตัวเลข ID ประจำแถวล่วงหน้าทันทีเพื่อความเร็วสูง
* **พฤติกรรมสัจจะ:** แม้ว่าธุรกรรมในวินาทีนั้นจะติดเอเรอร์เอฟเค (Foreign Key Violated) หรือถูกระบบสั่ง Rollback ทำลายแถวทิ้งกลางคัน ตัวเลข ID ที่ถูกแจกและนับแต้มไปแล้วจะไม่ถูกนำกลับมาหมุนเวียนใช้อีกเป็นอันขาด เพื่อรับประกันวินัยความปลอดภัยไม่ให้เลขชนกัน ข้อมูลแถวถัดไปจึงจะกระโดดข้ามเลขค้างรอบเสมอ (เช่น จากเลข 1 ข้ามไปเลข 2) ซึ่งเป็นพฤติกรรมปกติเกรดอุตสาหกรรม ไม่ใช่ข้อผิดพลาดของแอปพลิเคชัน

#### 5.7 ข้อจำกัดของ Unique Key ข้ามมิติ (Composite Unique Key Interpretation)
* **พฤติกรรมระดับถังข้อมูล:** การกำหนดคำสั่ง `@@unique([flight_id, user_id])` จะคุ้มครองความปลอดภัยดักจับข้อมูลซ้ำ **เฉพาะภายในกลุ่มก๊วนไอดีเดียวกันเท่านั้น** * **ตัวอย่างสัจจะ:** ฐานข้อมูลจะยอมให้ `user_id: 5` ไปปรากฏตัวซ้ำทั้งใน `flight_id: 3` (ก๊วน B) และ `flight_id: 4` (ก๊วน C) ได้พร้อมกัน เพราะในมุมมองคณิตศาสตร์ คู่หู `[3, 5]` กับ `[4, 5]` ถือเป็นข้อมูลคนละชุดกัน การป้องกันนักกอล์ฟเดินตีซ้ำสองก๊วนในเวลาเดียวกันจึงต้องยกยอดไปเป็นวินัยคุมระดับบน (Application Layer / Zod Check) ในเฟสถัดไป