# 🎯 กฎเหล็กและสถาปัตยกรรมการทำงานร่วมกัน (Golf-TD Project Working Rules)

ไฟล์นี้สร้างขึ้นเพื่อเตือนสติ AI (เจ็มคุง) ในการเสนอลอจิกและการเขียนโค้ดร่วมกับป๋าปู เพื่อป้องกันอาการ "หลอน คิดไปเอง" และลดการเสียเวลาโดยไม่จำเป็น

## 1. ปรัชญาการเลือกวิธีแก้ปัญหา: Simply VS Best Practice
* **กฎข้อที่ 1:** ให้เลือกวิธีที่ "เรียบง่าย ตรงไปตรงมา และยึดคู่มือมาตรฐานทางการ (Official Docs/Simply Standard)" มาก่อนการพยายามเขียนโค้ดซับซ้อนที่คิดขึ้นมาเองเสมอ
* **กฎข้อที่ 2:** ห้าม AI เขียนโค้ดแบบ "คิดดักหน้างานเอง" หรือพยายามสร้าง Object/Interface แปลกประหลาดขึ้นมาเองกลางอากาศ หากสิ่งนั้นไม่มีระบุไว้ในคู่มือของ Library เวอร์ชันนั้น ๆ
* **กฎข้อที่ 3:** ยึดพิมพ์เขียวสถาปัตยกรรมและคำแนะนำที่ผ่านการพิสูจน์แล้วจาก Claude เป็นหลักในการเดินหมากรหัส

## 2. กระบวนการตรวจสอบสิทธิ์ (Error Debugging Flow)
* เมื่อเกิดข้อผิดพลาด (TypeScript Error / Runtime Error) ห้ามเดาโค้ดชุดใหม่ไปเรื่อย ๆ แบบพายเรือในอ่าง
* ให้ AI วิเคราะห์จากบรรทัดที่ฟ้องเอเรอร์ (Error Log) อย่างตรงไปตรงมา และแก้ปัญหาโดยอ้างอิงจากระเบียบของไทป์ข้อมูลจริงในโปรเจกต์เท่านั้น

## 3. บริบทความต่างของเครื่องมือ
* ให้ระลึกเสมอว่าโปรเจกต์นี้ทำงานอยู่บนสภาพแวดล้อม:
  * ฝั่งพัฒนา (Station): เครื่อง Mac Air (pp2) ใช้ Node.js / TypeScript 6 / Prisma 7
  * ฝั่งฐานข้อมูล (Server): เครื่อง Windows (pp1) มุดท่อผ่านอุโมงค์พอร์ต 3307
* โค้ดทุกบรรทัดต้องออกแบบมาให้ทำงานสอดคล้องกับระบบเน็ตเวิร์กมุดท่อนี้อย่างเรียบง่ายที่สุด

## 4. โครงสร้างมาตรฐานโฟลเดอร์และการเดินท่อระบบ (Architecture Design Standard)
เพื่อความเป็นระเบียบสากลนิยม (Enterprise Standard) โครงสร้างการแบ่งไฟล์ระบบหลังบ้านจะถูกจัดสัดส่วนเด็ดขาดดังนี้:

### 📁 4.1 สถาปัตยกรรม 5 ขุนพลฝั่งผู้ควบคุม (Role-Based Controllers)
ให้แยกโฟลเดอร์ย่อยภายใน `src/controllers/` ตามระดับสิทธิ์และการใช้งานหลัก 5 ด้าน:
1. `auth/` -> ระบบตรวจสอบสิทธิ์, สมัครสมาชิกหน้าเว็บ (เช่น registerUser, login)
2. `admin/` -> ผู้ดูแลระบบสูงสุด ถือค้อนคุมนโยบายหลังบ้าน (เช่น addGolfer, registerCourse)
3. `td/` -> นายสนามคุมแมตช์การแข่งขัน (Tournament Director Management)
4. `scorer/` -> เจ้าหน้าที่หรือแคดดี้ผู้คีย์คะแนนดิบในสนาม
5. `user/` -> ตัวนักกอล์ฟทั่วไปสำหรับเรียกดูคะแนนหรือผลลัพธ์

### 🛣️ 4.2 ระเบียบการตั้งชื่อและการเปิดท่อส่งสัญญาน (Routing System Flow)
* **การตั้งชื่อโมดูล (Module Naming):** ให้ระบุการกระทำพ่วงท้ายให้ชัดเจนเสมอ ห้ามตั้งชื่อซ้ำซ้อนกันเด็ดขาด (เช่น `registerUser`, `registerCourse`, `registerTournament`)
* **ระบบท่อรวมศูนย์ (Main Router):** * ห้ามนำท่อย่อยไปประกาศพ่นรกไว้ที่ไฟล์ด่านหน้าอย่าง `server.ts` เด็ดขาด
  * ให้สร้างไฟล์ `src/routers/main.routes.ts` ทำหน้าที่เป็น "สถานีจ่ายไฟกลาง" เพื่อรวบรวมท่อย่อยทั้งหมด (`adminRouter`, `authRouter`, ฯลฯ)
  * จากนั้นในไฟล์ `src/server.ts` จะเหลือเพียงการเรียกใช้งานสั้น ๆ บรรทัดเดียวคือ: `app.use('/api/v1', mainRouter);`

---

## 5. มาตรฐานวงจรชีวิตของตัวควบคุม (Controller Lifecycle Standard)
โค้ดทุกฟังก์ชันภายในโฟลเดอร์ `controllers` จะต้องเขียนประดับคอมเมนต์และล็อกสเต็ปการไหลของข้อมูลตามลำดับ 1 ถึง n+1 อย่างเคร่งครัดดังนี้:

```typescript
export const registerSomething = async (req: Request, res: Response) => {
  // ============================================================
  // 1. REQUEST MANAGEMENT (จัดการแกะกล่องนำเข้าข้อมูลจากหน้าด่าน req.body)
  // ============================================================
  const { data_field } = req.body;

  // ============================================================
  // 2. VALIDATION & 3. ERROR HANDLING (ตรวจตราคุณสมบัติและดักจับข้อผิดพลาด)
  // ============================================================
  // ดักเช็คค่าห้ามว่างเปล่าล่วงหน้า เพื่อป้องกันคอมพิวเตอร์ชั้นในพ่น Error ยาวเหยียด
  if (!data_field) {
    throw createError(400, "ข้อความคำเตือนภาษาไทยสั้น ๆ กระชับตรงประเด็นประจำตัวป๋าปู");
  }

  // ============================================================
  // 4. ACTION STEPS (ขั้นตอนปฏิบัติการสลักข้อมูลลงฐานข้อมูลผ่าน Prisma)
  // ============================================================
  const result = await prisma.modelName.create({ data: { data_field } });

  // ============================================================
  // n+1. RESPONSE MANAGEMENT (สรุปผลสำเร็จส่งสถานะสีเขียว 200/201 กลับหน้าบ้าน)
  // ============================================================
  res.status(201).json({ success: true, message: "สำเร็จ", data: result });
};

### ⏰ 5.1 มาตรฐานการบันทึกแสตมป์เวลา (Audit Trail Standard)
เพื่อสัจจะแห่งข้อมูลและการทวนสอบ (Audit Trail) ในระบบเกรด Enterprise:
1. ทุกตารางข้อมูล (ทุกโมเดล) ในโปรเจกต์นี้ จะต้องมีฟีลด์กำกับเวลาสร้างและเวลาแก้ไขล่าสุดเสมอ
2. ในช่วงเฟสพึ่งตั้งไข่ เพื่อป้องกันบั๊กข้อมูลเก่าค้างแคชชนกับคอลัมน์ใหม่ (Prisma Migration Constraint) ให้ใช้ Syntax มาตรฐานดังนี้พ่วงท้ายในทุกโมเดล:
   * `created_at DateTime @default(now())`
   * `updated_at DateTime @default(now()) @updatedAt`

